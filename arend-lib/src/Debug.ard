-- Defines a `runTimed` meta that measures execution time of a computation `m`, normalizes it, prints the elapsed time, and returns the result.
\import Debug.Meta
\import Meta

\meta runTimed m => run {
  \let t => time,
  \let x => nf m,
  println (time Nat.- t),
  x
}